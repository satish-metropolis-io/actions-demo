name: My First Workflow

on: push

jobs:
    first_job:
        runs_on: ubuntu-latest
        steps:
          - name: Welcome Message
            run: echo "Hi, Welcome!!"

          - name: List Files
            run: ls

          - name: Read File
            run: cat README.md
